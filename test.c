#include "philo_bonus/philo_bonus.h"

void print()
{
	int a[1000000000000000];
	// printf("hello");
}

int	main(int ac, char **av) {
	int a[1];
	print();
	// int pid;
	// int status;
	// sem_t *var;

	// sem_unlink("var");
	// var = sem_open("var", O_CREAT, 0644, 1);
	// if (var == SEM_FAILED)
	// 	printf("erorr");
	// signal(SIGINT, print);
	// pid = fork();
	// if (pid == 0)
	// {
	// 	// sem_wait(var);
	// 	signal(SIGINT, print);
	// 	kill(getppid(), SIGINT);
	// 	usleep(1000000);
	// }
	// else{
	// 	kill(pid, SIGINT);
	// }
	// waitpid(-1, &status, 0);
	return (0);
}
